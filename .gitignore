# Plugin zips — never commit, trigger AV heuristics and bloat the repo
inc/plugins/*.zip
inc/plugins/**/*.zip

# General zip files
*.zip

# Node / build
node_modules/
npm-debug.log

# Source maps — dev only, not needed on production
*.map

# Bootstrap test suite — dev only, never needed at runtime
assets/scss/bootstrap/tests/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.suo
*.user
